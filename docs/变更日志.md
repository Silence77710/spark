# 变更日志

> 本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。
>
> 类型说明：`feat` 新增 / `fix` 修复 / `refactor` 重构 / `perf` 性能 / `docs` 文档 / `chore` 工程 / `revert` 回滚 / `security` 安全 / `breaking` 不兼容变更。

## [Unreleased]

### 文档规范
- docs: 6 篇工程文档全部改用中文命名（概览/业务/接口/数据模型/部署/变更日志）
- docs: 新增 `docs/架构.md`（技术架构文档：栈/分层/AI 集成/构建运行/依赖图）
- docs: 新增 `docs/规范.md`（工程级 rules：内容变化及时更新文档）
- docs: 更新交叉引用（AGENTS.md / SPEC.md / docs 内互引）

### Added
- docs: 新增 `docs/` 工程文档目录与 `概览.md` 索引
- docs: 新增 `docs/业务.md`（业务文档：领域、流程、状态机）
- docs: 新增 `docs/接口.md` + `docs/数据模型.md`（接口契约 + 数据模型附录）
- docs: 新增 `docs/变更日志.md`（变更日志）
- docs: 新增 `docs/部署.md`（打包部署文档）
- AGENTS.md: 文档同步硬规则增加 4 类工程文档

### Notes
- 尚未发版前的所有变更汇总在此，发布前整体归并到下一个版本号。

## [0.1.0] - 2026-08-19

### Changed (refactor / chore)
- `25c248f` refactor(config): 统一抽取状态等配置至共享模块 (`apps/web/src/lib/config.ts` + `@spark/utils`)
- `ce1caa5` chore(docker): 支持本地镜像运行，文档同步为 MySQL 现状（引入 `Dockerfile`，standalone 模式，AGENTS.md 更新）

### Added (feat)
- `f30ab81` feat(web): 添加创意重要程度支持及 AI 辅助功能（5 级 importance、AI features 设置、AI 路由批量）
- `2c1efd0` feat(idea): 列表支持状态筛选、排序与分页（`/api/ideas` 多条件查询）
- `aa50424` feat(idea): 添加活动时间线功能及自动记录活动（`idea_activities` 表 + `/api/ideas/[id]/activities`）
- `9622930` feat: add Markdown support for idea input and rendering（react-markdown + remark-gfm）

### Chore
- `531c683` chore(git): 添加 .idea 到忽略列表

### Notes
- 首次具备完整可演示能力的"种子"版（V1 MVP）。未发布前所有变更统一合并到 0.1.0 节点。
- 数据库从 SQLite（apps/web/spark.db）迁移到 MySQL（spark-mysql 容器）— 完成于 `ce1caa5`，涉及 `packages/@spark/db` 重写与 `schema.sql` 替换。

