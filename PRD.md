# Spark — 想法操作系统 · 产品需求文档

> PRD v5.0 · 2026-08-18 · 由 Senior PM 产出
> 关联文档：PRODUCT.md（产品定位）、SPEC.md（技术规格）、PRD-AI.md（AI 模块详细 PRD）

---

## 一、文档信息

| 字段 | 内容 |
|------|------|
| 产品名称 | Spark — 想法操作系统 |
| 文档版本 | v5.0 |
| 状态 | Active |
| 负责人 | Codex (Senior PM) |
| 目标读者 | 产品 / 设计 / 开发 |
| 变更摘要 | v4.0→v5.0：P3/P4 功能实现状态与代码同步——F25 思考节律、F26 看板、F27 语义搜索、F29 思考风格画像、F34 盲区地图 标记为已实现并补充详细规格；F23 思考镜子、F31 反方辩手、F32 验尸官、F33 跨界翻译 标记为 API 就绪待 UI 集成；更新 API 清单（新增 6 个 AI 端点）、AI Provider 架构（新增 6 个模块文件）、信息架构（新增 4 个页面路由）、AI 功能开关矩阵、设计规范（整合筛选 UX 改进、卡片颜色一致性、列表刷新策略等决策）；更新成功指标和迭代策略 |

---

## 二、产品概述

### 2.1 一句话定义

**Spark 是一款帮你捕捉、孵化、连接想法，最终把想法变成现实的思想花园。**

### 2.2 核心问题

想法的最大敌人不是"没有好想法"，而是四个系统性摩擦：

1. **捕获门槛太高** — 灵感转瞬即逝，等打开 App 已经忘了
2. **整理太累** — 花在分类上的时间比回看多，最终"存了等于想了"
3. **缺乏回访机制** — 想法像植物，不浇水就死，大多数想法存完就尘封
4. **连接缺失** — 最好的想法诞生于两个想法的交叉点，但线性列表让你看不到

### 2.3 设计哲学

| 原则 | 含义 |
|------|------|
| 捕获先于整理 | 写下来先于归类好，importance 默认为 0 |
| 渐进式结构化 | 刚录入只需标题+内容，后续慢慢补充 |
| 安静但不忘提醒 | 不打扰你，但定期温柔地提醒你回看 |
| 你的想法，你的数据 | 数据存储在本地 MySQL，开放不锁定 |
| AI 催化思考，不替代思考 | AI 是按需邀请的伙伴，默认关闭 |
| 想法有生命 | 每个想法有状态、健康度、血脉——它是生命体不是静态笔记 |
| 视觉一致性 | 卡片颜色由状态/重要程度定义，不因筛选状态而变化 |

### 2.4 竞品定位

| 维度 | Spark | Notion | Mem.ai | Obsidian |
|------|-------|--------|-------|----------|
| 核心定位 | 想法生长系统 | 文档协作 | AI 笔记 | 知识库 |
| AI 角色 | 思考伙伴（追问/发现连接/催化碰撞/反方辩手/验尸官/跨界翻译） | 内容生成 | 自动关联+聊天 | 插件依赖 |
| 想法生命周期 | 有状态（种子→萌芽→生长→实现/归档） | 无 | 无 | 无 |
| 想法健康度 | 综合活力指标（📋 规划中） | 无 | 无 | 无 |
| 主动发现连接 | AI 连接器（核心功能） | 无 | 自动关联 | 无 |
| 想法杂交 | AI 催化剂+遗传变异（独有） | 无 | 无 | 无 |
| 思考模式洞察 | AI 思考镜子+思考风格画像+思考节律 | 无 | 无 | 无 |
| 思维盲区检测 | AI 盲区地图（✅ 已实现） | 无 | 无 | 无 |
| 时间维度 | 胶囊+回望+血脉 | 无 | 无 | 无 |
| 数据隐私 | 本地优先+按需发送 | 云端 | 云端 | 本地 |

**差异化**：Spark 围绕"想法生命周期"这一独有数据结构，产生竞品做不到的洞察——连接、杂交、血脉、思考模式、健康度、盲区。

---

## 三、用户画像

### 3.1 目标用户

| 维度 | 描述 |
|------|------|
| 角色 | 创作者 / 独立开发者 / 产品人 / 内容创作者 |
| 身份 | 唯一用户（Spark 是个人工具，不做多用户协作） |
| 使用场景 | 灵感闪现时快速捕获；定期回看孵化想法；在想法间寻找连接和碰撞 |
| 核心目标 | 把碎片灵感发展成可执行的想法，最终变成现实 |
| 痛点 | 想法存了就忘；看不出想法之间的联系；写到一半就搁置；不知道自己的思考模式；看不出哪些想法值得继续投入 |
| 技术熟练度 | 中高（能使用本地 Docker、理解 Markdown） |
| AI 接受度 | 中等——欢迎 AI 辅助思考，但反感 AI 替代思考 |

### 3.2 非目标用户

- 团队协作用户
- 纯笔记用户（Notion/Obsidian 更合适）
- AI 重度依赖用户（期望 AI 直接生成内容的人）

### 3.3 用户旅程

**旅程 A：新用户首次使用**
打开 Spark → 空状态提示"写下第一个想法" → 输入标题 → Enter → 卡片出现在列表顶部 → 感受到零摩擦的快感

**旅程 B：日常闪电捕获**
灵感闪现 → 打开 Spark → 顶部输入框快速输入 → Enter 保存 → 关闭，继续工作

**旅程 C：回看与孵化**
打开主页 → 浏览列表 / 使用透镜筛选 → 点击感兴趣的卡片 → 查看时间线 → 在底部添加笔记或调研记录 → 调整状态（种子 → 萌芽）或重要程度

**旅程 D：AI 催化思考（需开启 AI）**
保存想法 → AI 追问一个有深度的问题 → 思考并回答（存为活动） → 或点击"下次再说"跳过

**旅程 E：AI 发现连接（需开启 AI）**
点击"发现连接" → AI 展示 1-3 对潜在连接 → 阅读解释 → 确认有价值的关联 / 忽略不合适的

**旅程 F：想法杂交（需开启 AI）**
点击"杂交台" → AI 展示两个来自不同领域的想法 + 催化说明 → 灵感碰撞 → 写下新想法（自动标记亲本） → 或"换一对" / "跳过"

**旅程 G：时间胶囊回望**
创建时勾选"时间胶囊" → 等待解锁日 → 胶囊解锁 → 打开详情页 → 与 AI 对话回望（需开启 AI） → 写下"现在的我怎么看"

**旅程 H：放弃与墓志铭**
将想法状态改为"已归档" → 弹出墓志铭输入 → 写下放弃原因 → 想法进入墓地 → 日后在墓地回看决策模式

**旅程 I：思考自我认知**
打开回顾中心 → 查看每日闪卡 + 本周摘要 + 遗忘预警 → 打开思考节律（/rhythm）→ 查看捕获热力图（发现自己总在深夜产出）→ 状态前进率 → 重要程度/情绪分布 → 点击"检测思维盲区" → AI 返回盲区报告 → 打开思考风格画像（/profile）→ AI 生成叙事性画像 + 风格标签

**旅程 J：想法健康管理（V4）**
打开想法详情 → 查看健康度分数（85/100，活跃度高但连接少） → AI 建议"这个想法可以和 X 关联" → 建立连接后健康度上升 → 在看板中追踪从"想做"到"已做"的进展

**旅程 K：看板行动管理**
打开 /kanban → 4 列看板展示（种子/萌芽/生长中/已实现） → 按集合筛选 → 用箭头按钮左右推进状态 → 点击卡片跳转详情页 → 追踪想法从"想"到"做"的进展

**旅程 L：语义搜索与发现**
打开 /explore → 输入自然语言查询 → 切换语义/关键词模式 → 语义搜索理解意图返回匹配 → 查看匹配理由和相似度 → 点击结果跳转详情

---

## 四、功能需求总览

### 4.1 功能全景

```
A. 闪电捕获        — 零摩擦录入                         ✅ 已完成
B. 想法卡片        — 有状态、有生命周期的想法实体       ✅ 已完成
C. 想法图谱        — 可视化思想网络                     ✅ 已完成
D. 回访系统        — 对抗遗忘，让想法被持续看见         ✅ 已完成
E. 轻量行动系统    — 从"想"到"做"的最小路径             ✅ 已完成（看板）
F. 搜索与发现      — 全文搜索 + 语义搜索                ✅ 已完成
G. AI 思考伙伴     — 催化思考而非替代思考               ✅ P0-P2 已完成 / 🚧 P3-P4 API 就绪
H. 想法关联        — 手动+AI 辅助建立想法间连接         ✅ 已完成
I. 想法健康度      — 综合活力指标                       📋 P4（未实现）
J. 思考自我认知    — 节律+风格画像+盲区地图+生态位      ✅ 节律+画像+盲区已完成
```

### 4.2 优先级矩阵

| 优先级 | 定义 | 功能 |
|--------|------|------|
| P0 Must Have | V1 MVP 必须完成 | F01-F10 ✅ 全部完成 |
| P1 Should Have | V2 核心扩展 | F11-F14 ✅ 全部完成 |
| P2 Could Have | V2 非功能扩展 | F15-F22 ✅ 全部完成 |
| P3 (V3 生长) | V3 生长阶段 | F23 AI 思考镜子 🚧、F24 想法血脉 📋、F25 思考节律 ✅、F26 看板视图 ✅、F27 语义搜索 ✅ |
| P4 (V4 花园) | V4 花园阶段 | F28 想法健康度 📋、F29 思考风格画像 ✅、F30 灵感生态位 📋、F31 AI 反方辩手 🚧、F32 AI 想法验尸官 🚧、F33 AI 跨界翻译 🚧、F34 思维盲区地图 ✅、F35 想法遗传与变异 📋 |

### 4.3 实现进度

| 功能 | 优先级 | 状态 | 备注 |
|------|--------|------|------|
| F01-F10 | P0 | ✅ 已实现 | 闪电捕获、卡片列表、重要程度、详情页、搜索、AI 苏格拉底、时间胶囊、墓志铭、墓地、AI 隐私基础版 |
| F11-F14 | P1 | ✅ 已实现 | AI 连接器、杂交台、回望对话、AI 隐私完整版 |
| F15-F22 | P2 | ✅ 已实现 | 想法图谱、回访系统、关联建立、情绪标记、集合管理、链接语法、老化提示、快速操作 |
| F23 AI 思考镜子 | P3 | 🚧 API 就绪 | mirror.ts + /api/ai/mirror 已实现；retro 页面 UI 集成待完成 |
| F24 想法血脉 | P3 | 📋 未实现 | 演化路径可视化（API 待开发） |
| F25 思考节律 | P3 | ✅ 已实现 | /rhythm 页面，含捕获热力图、状态前进率、重要程度/状态/情绪分布、活动频率趋势 |
| F26 看板视图 | P3 | ✅ 已实现 | /kanban 页面，4 列看板，箭头移动状态 |
| F27 语义搜索 | P3 | ✅ 已实现 | /explore 页面，语义+关键词双模式，AI 排序 |
| F28 想法健康度 | P4 | 📋 未实现 | 综合活力分数（API 待开发） |
| F29 思考风格画像 | P4 | ✅ 已实现 | /profile 页面 + /api/ai/profile，叙事性画像+风格标签 |
| F30 灵感生态位 | P4 | 📋 未实现 | 径向布局可视化思考版图 |
| F31 AI 反方辩手 | P4 | 🚧 API 就绪 | devil.ts + /api/ai/devil 已实现；详情页 UI 集成待完成 |
| F32 AI 想法验尸官 | P4 | 🚧 API 就绪 | coroner.ts + /api/ai/coroner 已实现；墓地页 UI 集成待完成 |
| F33 AI 跨界翻译 | P4 | 🚧 API 就绪 | translator.ts + /api/ai/translate 已实现；详情页 UI 集成待完成 |
| F34 思维盲区地图 | P4 | ✅ 已实现 | blindspot.ts + /api/ai/blindspot，集成在 /rhythm 页面 |
| F35 想法遗传与变异 | P4 | 📋 未实现 | 杂交后代继承+变异 |

---

## 五、P0 功能详细需求（已完成）

### F01 闪电捕获

| 属性 | 内容 |
|------|------|
| 目标 | 零摩擦的录入体验 |
| 入口 | 主页顶部固定输入框 |
| 交互 | 输入后 Enter 提交，Shift+Enter 换行 |
| 校验 | 标题不能为空，自动去除首尾空白 |
| 保存后 | 清空输入框，列表顶部出现新卡片 |
| 格式 | 纯文本 + Markdown |
| 胶囊选项 | 展开后可勾选「设为时间胶囊」，选择解锁日期（30天/90天/1年） |
| 集合选项 | 展开后可选择所属集合或新建集合 |
| 情绪选项 | 展开后可选择情绪标记（兴奋/好奇/焦虑/平静/困惑） |
| 活动记录 | 捕获时自动记录"捕获了想法「标题」" |
| API | `POST /api/ideas` |
| 状态 | ✅ 已实现 |

### F02 想法卡片列表（时间线视图）

| 属性 | 内容 |
|------|------|
| 筛选交互 | 「透镜 + 筛选弹层 + 激活条件标签」模式 |
| 透镜行 | `全部` / `重点`（importance 3-4）/ `未整理`（importance 0），一键切换重要程度视角 |
| 筛选弹层 | 点击「筛选」按钮打开，内含重要程度、状态、集合的多选 chip |
| 激活条件标签 | 有筛选条件时，在透镜行下方显示可单独移除的 token |
| 排序 | 默认"重要优先"（importance DESC, created_at DESC）；可切换最新/最早/最近更新/按状态 |
| 分页 | 每页 20 条，滚动加载更多 |
| 卡片内容 | 标题、内容预览（前 100 字）、状态标签、重要程度标签（>0 时）、集合标签、创建时间 |
| 密封胶囊 | 锁图标 + 倒计时，不显示 content |
| 刷新体验 | 筛选/排序变更时保留已有列表原样，数据返回后静默替换；仅首次加载显示骨架屏；避免列表晃动 |
| 卡片颜色一致性 | 卡片颜色由状态/重要程度定义，不因筛选状态而变浅，确保视觉一致性 |
| 状态 | ✅ 已实现 |

### F03 重要程度

| 属性 | 内容 |
|------|------|
| 默认等级 | 0-4 五级：未评级 / 灵感碎片 / 有意思 / 想做 / 必做 |
| 捕获时 | 默认为 0，不强制选择 |
| 列表页 | 卡片右上角显示等级标签（0 不显示）；支持多选筛选；透镜预设 |
| 详情页 | 下拉框随时调整 |
| 变更记录 | 变化自动记录到活动时间线 |
| 个性化 | `/settings` 支持自定义每个等级的名称和描述，保存到 settings 表 importance_levels |
| 色彩体系 | 0=灰、1=slate、2=amber、3=orange、4=rose，可视化递进 |
| 状态 | ✅ 已实现 |

### F04-F10 概要（均已实现）

| 编号 | 功能 | 要点 |
|------|------|------|
| F04 | 想法详情页 | 路由 /ideas/[id]，Markdown 渲染，状态/重要程度下拉，活动时间线，⌘+↵ 快捷键 |
| F05 | 基础搜索 | 实时搜索（防抖 300ms），LIKE 搜索 title + content |
| F06 | AI 苏格拉底 | 保存非胶囊 idea 后追问一个有深度的问题，可答可跳，API 失败静默降级 |
| F07 | 时间胶囊 | 密封+解锁日期，解锁前隐藏内容，列表显示锁图标+倒计时 |
| F08 | 想法墓志铭 | 归档/休眠时弹非模态输入框，存为 general 活动 |
| F09 | 想法墓地 | /graveyard 路由，展示已归档/休眠想法+墓志铭 |
| F10 | AI 隐私基础版 | 总开关+隐私说明 |

---

## 六、P1 功能详细需求（已完成）

| 编号 | 功能 | 要点 |
|------|------|------|
| F11 | AI 连接器 | 主动发现想法间连接（>=5 条），embedding+LLM 解释，用户确认建边 |
| F12 | 想法杂交台 | AI 挑选碰撞对（>=4 条），碰撞生成新 idea（标记 parent_a/b），用户可换一对/跳过 |
| F13 | AI 回望对话 | 胶囊解锁后多轮对话，基于原始内容+活动时间线，用户写"现在的我怎么看"存档 |
| F14 | AI 隐私完整版 | 4 功能独立开关（socratic/connector/catalyst/retro）+ 数据发送日志（最近 20 条） |

---

## 七、P2 功能详细需求（已完成）

| 编号 | 功能 | 要点 |
|------|------|------|
| F15 | 想法图谱 | D3.js 力导向图，拖拽/缩放/筛选/图例，节点=想法边=关联 |
| F16 | 回访系统 | 每日闪卡翻转+本周摘要+遗忘预警（>30天未回看） |
| F17 | 关联手动建立 | 详情页搜索+类型选择+列表展示+删除，4 种类型 |
| F18 | 情绪标记 | 5 种情绪色点（兴奋/好奇/焦虑/平静/困惑），捕获+详情页 |
| F19 | 集合管理 | 重命名+删除+计数，API GET/PUT/DELETE |
| F20 | 链接语法 | [[]] 渲染为链接+反向链接区域 |
| F21 | 老化提示 | >30 天 opacity-75+未回看提示 |
| F22 | 快速操作 | 三点菜单+推进状态+设级+归档 |

---

## 八、P3 功能详细需求（V3 生长阶段）

### F23 AI 思考镜子（Thinking Mirror）

| 属性 | 内容 |
|------|------|
| 目标 | 用用户自己的数据生成关于思考模式的叙事性洞察 |
| 触发 | 每月 1 号自动检查 / 用户在 retro 页手动触发 |
| 前置条件 | idea 总数 >= 20 且使用时长 >= 30 天 |
| AI 行为 | 读取元数据（不含完整内容）：每条 idea 的 importance、status、created_at、updated_at、状态变更历史、活动频率、集合分布、情绪分布 |
| 输出 | 3-5 条叙事性洞察，每条附带一个可操作建议 |
| 洞察维度 | 重要程度实现率（如"你的'必做'实现率是 0%"）、状态前进率、捕获时段规律、集合偏好、情绪与状态关联、放弃模式 |
| 展示位置 | retro 页面"思考镜子"板块，不推送通知 |
| 语言风格 | 自然语言叙述，不用图表和数字堆砌，像朋友跟你聊天 |
| 降级 | 数据不足时提示"继续使用，积累更多想法后解锁"；API 失败时静默降级 |
| API | `POST /api/ai/mirror` ✅ 已实现 — 读取元数据统计，调用 AI 生成洞察 |
| AI 模块 | `packages/@spark/ai/src/mirror.ts` ✅ 已实现 — generateMirrorInsights + MirrorStats 类型 |
| 状态 | 🚧 API 就绪，retro 页面 UI 集成待完成 |

**交互流程：**
```
retro 页面加载
  -> 检查数据量：idea >= 20 且使用 >= 30 天
    -> 不足 -> 显示"继续使用，积累更多想法后解锁思考镜子"
    -> 足够 -> 检查本月是否已生成
      -> 已生成 -> 展示上次洞察
      -> 未生成 -> 显示"生成本月思考镜子"按钮
        -> 用户点击 -> loading "AI 正在阅读你的思考轨迹..."
        -> 读取元数据统计（不发送完整内容）
        -> AI 生成 3-5 条叙事性洞察
        -> 每条洞察：洞察文本 + 可操作建议（可选采纳）
        -> 展示在 retro 页面
```

**验收标准：**
- [ ] 数据不足时显示友好提示，不报错
- [ ] 洞察用自然语言叙述，不堆砌数字
- [ ] 每条洞察有可操作建议
- [ ] 洞察只读取元数据，不发送完整内容
- [ ] 不推送通知，用户自然访问时看到
- [ ] API 失败时静默降级
- [ ] retro 页面展示洞察板块（待 UI 集成）

### F24 想法血脉（Idea Lineage）

| 属性 | 内容 |
|------|------|
| 目标 | 可视化一个想法从碎片到实现的完整演化路径 |
| 位置 | 详情页"血脉"板块，可折叠，默认不展开 |
| 血脉内容 | 原始捕获 → 状态变更历史 → 关键活动（笔记/调研/决策）→ 关联建立 → 杂交后代 → 衍生关联 |
| 杂交亲本 | parent_a_id 和 parent_b_id 自动显示亲本链接 |
| 衍生关联 | type=derived 的关联自动纳入血脉 |
| 视觉形式 | 垂直时间轴，节点=事件，连线=演化路径，不同事件类型有不同图标 |
| 交互 | 点击事件节点可跳转到相关活动详情；点击亲本/后代链接跳转到对应想法 |
| 密封胶囊 | 胶囊未解锁时隐藏血脉 |
| API | `GET /api/ideas/:id/lineage` 📋 待开发 — 聚合活动+关联+亲本信息 |
| 状态 | 📋 未实现 |

**血脉数据结构：**
```typescript
interface LineageEvent {
  type: 'capture' | 'status_change' | 'importance_change' | 'note' | 'research' 
      | 'decision' | 'relationship' | 'hybrid_birth' | 'derived_from';
  timestamp: string;
  description: string;
  relatedIdeaId?: string;    // 跳转目标
  relatedIdeaTitle?: string;
}
```

**验收标准：**
- [ ] 血脉按时间正序排列（从最早到最新）
- [ ] 杂交产生的想法显示亲本链接
- [ ] 衍生关联纳入血脉
- [ ] 默认折叠，点击展开
- [ ] 点击亲本/后代链接可跳转
- [ ] 密封胶囊不显示血脉
- [ ] 关键活动有类型图标

### F25 思考节律（Thinking Rhythm）

| 属性 | 内容 |
|------|------|
| 目标 | 元数据统计面板，让用户看到自己的思考习惯 |
| 位置 | `/rhythm` 页面 |
| 数据来源 | 所有 idea 的元数据（不依赖 AI，纯前端计算） |
| 统计维度 | 捕获时段热力图、状态前进率、重要程度分布、状态分布、情绪分布、活动频率趋势 |
| 热力图 | 横轴=小时（0-23），纵轴=星期（周一-周日），颜色深浅=该时段的捕获数量 |
| 状态前进率 | seed→sprout+ 的比例，统计有多少想法走出了种子状态 |
| 重要程度分布 | 0-4 各等级的想法数量占比，色点可视化（灰/slate/amber/orange/rose） |
| 状态分布 | 6 种状态（种子/萌芽/生长中/已实现/已归档/休眠）的分布占比 |
| 情绪分布 | 5 种情绪的分布占比 |
| 活动频率 | 最近 30 天每日活动数量趋势线 |
| AI 盲区检测 | 集成 F34 思维盲区地图，用户可点击"检测思维盲区"获取 AI 报告 |
| AI 模块 | 集成 blindspot.ts detectBlindSpots |
| 状态 | ✅ 已实现 |

**验收标准：**
- [ ] 捕获热力图正确显示小时×星期的分布
- [ ] 热力图颜色深浅对应数量
- [ ] 状态前进率显示百分比
- [ ] 重要程度/状态/情绪分布有可视化图表
- [ ] 活动频率趋势线显示最近 30 天
- [ ] 无数据时显示友好空状态
- [ ] 不依赖 AI，纯前端计算
- [ ] 盲区检测按钮触发 AI 调用，结果展示在同一页面
- [ ] 盲区检测返回主题盲区、视角盲区、情绪盲区、探索建议

### F26 看板视图（Kanban View）

| 属性 | 内容 |
|------|------|
| 目标 | 从"想"到"做"的行动系统，用看板追踪想法进展 |
| 位置 | `/kanban` 页面 |
| 列定义 | 4 列：种子（seed）/ 萌芽（sprout）/ 培养中（growing）/ 已实现（realized） |
| 卡片内容 | 标题、重要程度色点、集合标签、创建时间 |
| 卡片操作 | 左右箭头按钮在列之间移动状态；点击卡片跳转详情页 |
| 集合筛选 | 顶部可按集合筛选（可哈希着色） |
| 排序 | 列内按重要程度降序 |
| 归档/休眠 | 不在看板中显示（在看板中只追踪活跃想法） |
| 状态变更 | 移动列即变更状态，自动记录 status_change 活动 |
| API | `GET /api/ideas?pageSize=999` 一次加载，前端按 status 分列 |
| 交互 | 不使用拖拽（移动端不友好），用箭头按钮左右移动 |
| 状态 | ✅ 已实现 |

**验收标准：**
- [ ] 4 列看板，列标题为状态名称
- [ ] 卡片有左右箭头按钮移动状态
- [ ] 移动后自动记录状态变更活动
- [ ] 顶部可按集合筛选
- [ ] 列内按重要程度降序
- [ ] 归档/休眠想法不在看板中
- [ ] 点击卡片跳转详情页
- [ ] 空列有友好提示
- [ ] 不使用拖拽，箭头按钮移动（移动端友好）

### F27 语义搜索（Semantic Search）

| 属性 | 内容 |
|------|------|
| 目标 | 基于语义的搜索，理解意图而非关键词 |
| 位置 | `/explore` 页面 |
| 行为 | 用户输入自然语言查询，AI 排序返回语义最接近的 idea |
| 双模式 | 语义模式（AI 排序）+ 关键词模式（LIKE 搜索），可切换 |
| 对比基础搜索 | 基础搜索用 LIKE 匹配关键词；语义搜索理解意图，如搜"怎么提高效率"能找到标题是"时间管理"的想法 |
| 结果排序 | 按语义相似度降序，每条结果显示相似度百分比和匹配理由 |
| 实现方式 | 获取全部 idea，AI 对每条计算语义相似度并排序，返回 reason + score |
| 混合模式 | 语义模式下结果含匹配理由；关键词模式回退到标准 LIKE 搜索 |
| 降级 | AI 不可用时回退到基础 LIKE 搜索 |
| API | `GET /api/ideas?q=xxx` 关键词模式 + AI 模块前端排序 |
| 状态 | ✅ 已实现 |

**验收标准：**
- [ ] 输入自然语言查询返回语义相关结果
- [ ] 结果按相似度排序，显示匹配理由
- [ ] AI 不可用时回退到 LIKE 搜索
- [ ] 语义/关键词模式可切换
- [ ] 结果显示匹配理由和相似度分数

---

## 九、P4 功能详细需求（V4 花园阶段）

### F28 想法健康度（Idea Health Score）

| 属性 | 内容 |
|------|------|
| 目标 | 综合活力指标，量化每个想法的"健康程度" |
| 位置 | 详情页 + /rhythm 页面统计 |
| 分数构成 | 活跃度（最近活动天数）+ 连接数 + 状态进展速度 + 更新频率 |
| 分值范围 | 0-100，新想法默认 50（活跃），无活动 30 天降至枯萎区（<30） |
| 预警 | 健康度 <30 时在详情页显示"这个想法在枯萎"提示 |
| 健康/枯萎 | >70 健康 / 40-70 一般 / <40 枯萎 |
| API | `GET /api/ideas/:id/health` 📋 待开发 — 计算健康度分数 |
| 数据模型 | ideas 表新增 `health_score INT` 字段 |
| 状态 | 📋 未实现 |

**验收标准：**
- [ ] 健康度分数 0-100，新想法默认 50
- [ ] 分数由活跃度+连接数+进展速度+更新频率加权计算
- [ ] <30 时显示"枯萎"提示
- [ ] /rhythm 显示健康度分布概览
- [ ] 推进状态/建立关联后健康度上升

### F29 思考风格画像（Thinking Profile）

| 属性 | 内容 |
|------|------|
| 目标 | AI 生成关于用户思考习惯的叙事性画像 + 风格标签 |
| 位置 | `/profile` 独立页面 |
| 触发 | 用户点击"生成思考画像"按钮，AI 开启时可用 |
| 前置条件 | idea 总数 >= 15 |
| AI 行为 | 读取所有 idea 的元数据（标题+内容+状态+重要程度+情绪+活动+关联），生成画像 |
| 输出 | 1 段叙事性画像 + 5 个维度分析 + 多个风格标签 |
| 画像维度 | 捕获节律、主题偏好、情绪模式、决策风格、连接习惯 |
| 风格标签 | 如"深夜思考者""广度探索者""完美主义者""连接型思考"等 |
| 展示形式 | 叙事文本 + 5 维度卡片（带图标）+ 彩色风格标签 |
| 降级 | 数据不足时提示"想法还不够多"；AI 不可用时提示"AI 暂时不可用" |
| API | `POST /api/ai/profile` ✅ 已实现 |
| AI 模块 | `packages/@spark/ai/src/profile.ts` ✅ 已实现 — generateThinkingProfile + ProfileStats 类型 |
| 状态 | ✅ 已实现 |

**验收标准：**
- [ ] 点击按钮触发 AI 生成画像
- [ ] 画像包含叙事文本 + 5 维度分析 + 风格标签
- [ ] 风格标签有颜色区分（哈希着色）
- [ ] 5 维度各有图标
- [ ] 数据不足时友好提示
- [ ] AI 不可用时友好提示

### F30 灵感生态位（Niche Map）

| 属性 | 内容 |
|------|------|
| 目标 | 径向布局可视化用户的思考版图 |
| 位置 | /rhythm 页面 / 独立页面 |
| 视觉形式 | 径向树状图，中心=用户，分支=主题领域，叶节点=具体想法 |
| 聚类方式 | 基于集合 + 关键词聚类 |
| 颜色编码 | 按领域着色，节点大小=想法成熟度 |
| 状态 | 📋 未实现 |

**验收标准：**
- [ ] 径向布局展示思考版图
- [ ] 中心为用户，分支为主题领域
- [ ] 按集合+关键词聚类
- [ ] 点击节点跳转详情页

### F31 AI 反方辩手（Devil's Advocate）

| 属性 | 内容 |
|------|------|
| 目标 | AI 站在对立面提出挑战，迫使思考更全面 |
| 位置 | 详情页"挑战"板块，AI 开启时显示 |
| 触发 | 用户点击"挑战这个想法"按钮 |
| AI 行为 | 读取 idea 的 title+content，生成 2-3 条反方论点 |
| 输出 | 反方论点列表，每条有挑战文本 |
| 用户操作 | 阅读 → 记录为活动 / 忽略 |
| 降级 | AI 不可用时不显示挑战按钮 |
| API | `POST /api/ai/devil` ✅ 已实现 — 生成反方辩手挑战 |
| AI 模块 | `packages/@spark/ai/src/devil.ts` ✅ 已实现 — generateDevilChallenges |
| 状态 | 🚧 API 就绪，详情页 UI 集成待完成 |

**验收标准：**
- [ ] 点击"挑战"按钮触发 AI
- [ ] 返回 2-3 条反方论点
- [ ] 每条论点清晰呈现
- [ ] 用户可记录或忽略
- [ ] AI 不可用时不显示按钮

### F32 AI 想法验尸官（Idea Coroner）

| 属性 | 内容 |
|------|------|
| 目标 | AI 分析已放弃想法的模式，帮用户理解决策习惯 |
| 位置 | 墓地页 / retro 页面 |
| 触发 | 用户点击"验尸报告"按钮 |
| 前置条件 | 已归档/休眠想法 >= 5 条 |
| AI 行为 | 读取所有已放弃想法的 title+epitaph+状态变更历史 |
| 输出 | 放弃模式分析报告（如"你倾向于在萌芽阶段放弃""情绪焦虑时更容易放弃"） |
| 降级 | 数据不足时提示"安葬更多想法后解锁验尸报告" |
| API | `POST /api/ai/coroner` ✅ 已实现 — 生成验尸报告 |
| AI 模块 | `packages/@spark/ai/src/coroner.ts` ✅ 已实现 — generateCoronerReport + DeceasedIdea 类型 |
| 状态 | 🚧 API 就绪，墓地页 UI 集成待完成 |

**验收标准：**
- [ ] 已放弃想法 >= 5 条时可触发
- [ ] 返回模式分析报告
- [ ] 报告用自然语言叙述
- [ ] 数据不足时友好提示
- [ ] 墓地页展示验尸报告入口（待 UI 集成）

### F33 AI 跨界翻译（Cross-Domain Translator）

| 属性 | 内容 |
|------|------|
| 目标 | AI 用其他领域的视角重新解读想法，催化跨界思考 |
| 位置 | 详情页"跨界视角"板块 |
| 触发 | 用户点击"跨界翻译"按钮 |
| AI 行为 | 读取 idea 的 title+content，用 3 个不同领域的视角重新解读 |
| 输出 | 3 条跨界解读，每条标注领域名称 |
| 领域示例 | 生物学视角、经济学视角、艺术视角、工程学视角、心理学视角 |
| 用户操作 | 阅读 → 记录为活动 / 忽略 |
| 降级 | AI 不可用时不显示翻译按钮 |
| API | `POST /api/ai/translate` ✅ 已实现 — 跨界翻译 |
| AI 模块 | `packages/@spark/ai/src/translator.ts` ✅ 已实现 — translateCrossDomain |
| 状态 | 🚧 API 就绪，详情页 UI 集成待完成 |

**验收标准：**
- [ ] 点击"跨界翻译"按钮触发 AI
- [ ] 返回 3 条跨界解读
- [ ] 每条标注领域名称
- [ ] 用户可记录或忽略
- [ ] AI 不可用时不显示按钮

### F34 思维盲区地图（Blind Spot Map）

| 属性 | 内容 |
|------|------|
| 目标 | AI 检测用户从未探索的领域、视角、情绪 |
| 位置 | `/rhythm` 页面，"思维盲区"板块 |
| 触发 | 用户点击"检测思维盲区"按钮 |
| 前置条件 | idea 总数 >= 15 |
| AI 行为 | 读取所有 idea 的元数据+关键词（不含完整内容），分析覆盖度 |
| 输出 | 主题盲区、视角盲区、情绪盲区、探索建议、已探索领域列表 |
| 主题盲区 | 如"从未写过关于人际关系/健康/财务的想法" |
| 视角盲区 | 如"从未从批判性/系统性/感性视角思考" |
| 情绪盲区 | 如"从未在平静状态下写想法" |
| 探索建议 | AI 建议的 3-5 个可探索方向 |
| API | `POST /api/ai/blindspot` ✅ 已实现 — 盲区检测 |
| AI 模块 | `packages/@spark/ai/src/blindspot.ts` ✅ 已实现 — detectBlindSpots + BlindSpotResult 类型 |
| 状态 | ✅ 已实现，集成在 /rhythm 页面 |

**验收标准：**
- [ ] 点击"检测思维盲区"按钮触发 AI
- [ ] 返回主题盲区、视角盲区、情绪盲区、探索建议
- [ ] 返回已探索领域列表
- [ ] 数据不足时友好提示
- [ ] AI 不可用时友好提示
- [ ] 结果展示在 /rhythm 页面

### F35 想法遗传与变异（Idea Genetics）

| 属性 | 内容 |
|------|------|
| 目标 | 杂交后代继承亲本特征并产生变异 |
| 位置 | 详情页 + 杂交台 |
| 依赖 | F12 想法杂交台（parent_a_id + parent_b_id） |
| 遗传规则 | 后代自动继承亲本的集合、重要程度中值、情绪 |
| 变异 | AI 建议变异方向（如"从 A 的工程视角 + B 的艺术视角 → 产品设计视角"） |
| 血脉展示 | F24 血脉视图中杂交后代显示遗传特征 |
| API | `POST /api/ai/genetics` 📋 待开发 |
| AI 模块 | `packages/@spark/ai/src/genetics.ts` 📋 待开发 |
| 状态 | 📋 未实现 |

**验收标准：**
- [ ] 杂交后代自动继承亲本集合
- [ ] 重要程度取亲本中值
- [ ] AI 建议变异方向
- [ ] 血脉视图中展示遗传特征

---

## 十、AI 整合策略

### 10.1 三层 AI 能力模型

```
反应层（Reactive）    — 用户操作后被动响应
  ├─ F06 苏格拉底追问（保存后追问）
  └─ F13 回望对话（胶囊解锁后对话）

主动层（Proactive）   — 用户主动触发，AI 提议
  ├─ F11 连接器（发现想法间连接）
  ├─ F12 杂交台（催化碰撞）
  ├─ F31 反方辩手（对立面挑战） 🚧 API 就绪
  └─ F33 跨界翻译（跨领域视角） 🚧 API 就绪

反思层（Reflective）  — AI 用用户数据生成自我认知
  ├─ F23 思考镜子（月度洞察） 🚧 API 就绪
  ├─ F29 思考风格画像（思考习惯画像） ✅
  ├─ F32 想法验尸官（放弃模式分析） 🚧 API 就绪
  └─ F34 思维盲区地图（未探索领域检测） ✅
```

### 10.2 AI 触发流程

```
用户操作
  ├─ 保存想法 ──────────────→ 苏格拉底追问（反应层）
  ├─ 点击"发现连接" ────────→ 连接器计算（主动层）
  ├─ 点击"杂交台" ──────────→ 催化剂配对（主动层）
  ├─ 胶囊解锁 ──────────────→ 回望对话（反应层）
  ├─ 点击"挑战" ────────────→ 反方辩手（主动层） 🚧
  ├─ 点击"跨界翻译" ────────→ 跨界翻译（主动层） 🚧
  ├─ retro 页面访问 ────────→ 思考镜子（反思层） 🚧
  ├─ graveyard 页面访问 ────→ 想法验尸官（反思层） 🚧
  └─ rhythm 页面访问 ───────→ 盲区地图（反思层） ✅
  
  所有 AI 调用 ─→ ai_interactions 表记录日志
  所有 AI 调用 ─→ 检查 ai_enabled + 分功能开关
  所有 AI 调用 ─→ 失败时静默降级
```

### 10.3 AI 功能开关矩阵

| AI 功能 | 默认 | 触发方式 | 发送数据 | 记录日志 | 状态 |
|---------|------|----------|----------|----------|------|
| 苏格拉底（F06） | 关 | 保存后自动 | idea 标题+内容 | ✅ | ✅ 已实现 |
| 连接器（F11） | 关 | 用户点击 | 所有 idea 标题+内容 | ✅ | ✅ 已实现 |
| 杂交台（F12） | 关 | 用户点击 | 所有 idea 标题+内容 | ✅ | ✅ 已实现 |
| 回望对话（F13） | 关 | 用户点击 | 胶囊内容+活动历史 | ✅ | ✅ 已实现 |
| 思考镜子（F23） | 关 | 用户点击 | 元数据统计（无完整内容） | ✅ | 🚧 API 就绪 |
| 反方辩手（F31） | 关 | 用户点击 | idea 标题+内容 | ✅ | 🚧 API 就绪 |
| 验尸官（F32） | 关 | 用户点击 | 已放弃想法标题+墓志铭 | ✅ | 🚧 API 就绪 |
| 跨界翻译（F33） | 关 | 用户点击 | idea 内容 | ✅ | 🚧 API 就绪 |
| 盲区地图（F34） | 关 | 用户点击 | 元数据+关键词（无完整内容） | ✅ | ✅ 已实现 |
| 风格画像（F29） | 关 | 用户点击 | 元数据统计（无完整内容） | ✅ | ✅ 已实现 |

### 10.4 AI Provider 架构

```
packages/@spark/ai/
  src/
    provider.ts        # Provider 抽象 + extractJson 工具 ✅
    socratic.ts        # 反应层：苏格拉底追问 ✅
    connector.ts       # 主动层：连接发现（embedding + LLM） ✅
    catalyst.ts        # 主动层：杂交配对选择 ✅
    retro.ts           # 反应层：回望对话 ✅
    mirror.ts          # 反思层：思考镜子洞察 🚧
    devil.ts           # 主动层：反方辩手 🚧
    coroner.ts         # 反思层：想法验尸官 🚧
    translator.ts      # 主动层：跨界翻译 🚧
    blindspot.ts       # 反思层：盲区检测 ✅
    profile.ts         # 反思层：思考风格画像 ✅
    genetics.ts        # 主动层：遗传与变异 📋 待开发
  index.ts
```

API 路由：

```
/api/ai/socratic         POST  -> 生成追问          ✅
/api/ai/connect          POST  -> 发现连接          ✅
/api/ai/catalyst         POST  -> 杂交配对          ✅
/api/ai/retro            POST  -> 回望对话          ✅
/api/ai/interactions     GET   -> 交互日志          ✅
/api/ai/mirror           POST  -> 思考镜子洞察      🚧 API 就绪
/api/ai/devil            POST  -> 反方辩手          🚧 API 就绪
/api/ai/coroner          POST  -> 想法验尸官        🚧 API 就绪
/api/ai/translate        POST  -> 跨界翻译          🚧 API 就绪
/api/ai/blindspot        POST  -> 盲区地图          ✅ 已实现
/api/ai/profile          POST  -> 思考风格画像      ✅ 已实现
```

### 10.5 AI 降级策略

| 场景 | 处理方式 |
|------|----------|
| AI 总开关关闭 | 所有 AI 功能不显示，核心功能不受影响 |
| 分功能开关关闭 | 对应功能不显示，其他功能不受影响 |
| API 调用失败 | 静默降级，不报错不阻塞 |
| API 返回非法 JSON | 剥离 markdown 代码围栏后重试，仍失败则降级为空结果 |
| 数据不足 | 显示友好提示（"想法还不够多"），不报错 |
| 网络断开 | AI 功能不可用，核心功能正常 |

---

## 十一、信息架构

```
/                        — 想法流（默认视图，时间线 + 透镜筛选 + 集合导航）
/ideas/:id               — 单个想法详情（含血脉、遗传特征）
/ideas/:id/edit          — 编辑想法
/graveyard               — 想法墓地（含验尸报告）
/settings                — 偏好设置
/graph                   — 想法图谱
/retro                   — 回顾中心（每日闪卡、周报、遗忘预警）
/kanban                  — 看板视图（行动导向） ✅
/explore                 — 语义搜索与发现 ✅
/rhythm                  — 思考节律（捕获热力图、状态/情绪/重要程度分布、活动趋势、盲区地图） ✅
/profile                 — 思考风格画像 ✅
```

---

## 十二、数据模型

### ideas 表

| 字段 | 类型 | 说明 | 状态 |
|------|------|------|------|
| id | VARCHAR(36) | UUID PK | ✅ |
| title | VARCHAR(500) | 标题（必填） | ✅ |
| content | TEXT | 正文（Markdown） | ✅ |
| status | VARCHAR(20) | seed/sprout/growing/realized/archived/dormant | ✅ |
| collection | VARCHAR(100) | 所属集合 | ✅ |
| importance | TINYINT | 0-4 重要程度 | ✅ |
| is_capsule | BOOLEAN | 是否为时间胶囊 | ✅ |
| unlock_at | DATETIME(3) | 胶囊解锁时间 | ✅ |
| epitaph | TEXT | 墓志铭 | ✅ |
| parent_a_id | VARCHAR(36) | 杂交亲本 A | ✅ |
| parent_b_id | VARCHAR(36) | 杂交亲本 B | ✅ |
| emotion | VARCHAR(20) | 情绪标记 | ✅ |
| created_at | DATETIME(3) | 捕获时间 | ✅ |
| updated_at | DATETIME(3) | 最后修改时间 | ✅ |
| last_reviewed_at | DATETIME(3) | 最后回看时间 | ✅ |
| health_score | INT | 健康度分数 0-100 | 📋 P4 新增 |

### idea_activities 表

活动类型：capture / status_change / importance_change / note / research / discussion / prototype / decision / reference / general

### idea_relationships 表

关联类型：related / conflict / derived / parent_child
建立者：user / ai_suggested

### ai_interactions 表

功能（扩展）：socratic / connector / catalyst / retro / mirror / devil / coroner / translate / blindspot / profile / genetics

### settings 表

| 键 | 值格式 | 说明 | 状态 |
|------|--------|------|------|
| importance_levels | JSON 数组，5 项 | 自定义重要程度等级名称和描述 | ✅ 已实现 |
| ai_enabled | boolean | AI 总开关 | ✅ 已实现 |
| ai_features | JSON | 各功能开关 | ✅ 已实现（4 功能） |

### settings 表新增键（P3-P4）

| 键 | 值格式 | 说明 |
|------|--------|------|
| ai_features | JSON | 各功能开关扩展：{ socratic, connector, catalyst, retro, mirror, devil, coroner, translate, blindspot, profile }（现有 4 功能，需扩展至 10 功能） |

---

## 十三、API 清单

### 想法 CRUD

| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| GET | `/api/ideas` | 列表查询（q/collection/status/importance/sort/page/pageSize/backlinks/parent） | ✅ |
| POST | `/api/ideas` | 创建想法（支持 parent_a_id/parent_b_id 杂交、emotion 情绪标记） | ✅ |
| GET | `/api/ideas/:id` | 获取单个想法 | ✅ |
| PATCH | `/api/ideas/:id` | 更新想法（含 epitaph、emotion） | ✅ |
| DELETE | `/api/ideas/:id` | 删除想法 | ✅ |
| GET | `/api/ideas/:id/activities` | 获取活动时间线 | ✅ |
| POST | `/api/ideas/:id/activities` | 添加活动 | ✅ |
| GET | `/api/ideas/:id/lineage` | 获取想法血脉 | 📋 P3 |
| GET | `/api/ideas/:id/health` | 获取健康度分数 | 📋 P4 |

### 集合 / 关联 / 设置（均已实现）

| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| GET / PUT / DELETE | `/api/collections` | 集合管理 | ✅ |
| GET / POST / DELETE | `/api/relationships` | 关联管理 | ✅ |
| GET / PUT | `/api/settings` | 设置管理 | ✅ |

### AI 功能

| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| POST | `/api/ai/socratic` | 苏格拉底追问 | ✅ |
| POST | `/api/ai/connect` | 发现连接 | ✅ |
| POST | `/api/ai/catalyst` | 杂交配对 | ✅ |
| POST | `/api/ai/retro` | 回望对话 | ✅ |
| GET | `/api/ai/interactions` | AI 交互日志 | ✅ |
| POST | `/api/ai/mirror` | 思考镜子洞察 | 🚧 API 就绪 |
| POST | `/api/ai/devil` | 反方辩手 | 🚧 API 就绪 |
| POST | `/api/ai/coroner` | 想法验尸官 | 🚧 API 就绪 |
| POST | `/api/ai/translate` | 跨界翻译 | 🚧 API 就绪 |
| POST | `/api/ai/blindspot` | 盲区地图 | ✅ 已实现 |
| POST | `/api/ai/profile` | 思考风格画像 | ✅ 已实现 |

---

## 十四、技术架构

| 层面 | 选型 | 理由 |
|------|------|------|
| 前端框架 | Next.js (App Router) | SSR + 全栈能力 |
| 样式 | Tailwind CSS + shadcn/ui | 快速迭代，美观 |
| 数据存储 | MySQL 8（本机 Docker） | 数据自有 |
| 包管理 | pnpm workspace | monorepo |
| AI Provider | OpenAI 兼容接口（DeepSeek） | 可替换 |
| 图谱渲染 | D3.js | 力导向图交互 |
| 热力图渲染 | CSS Grid + Tailwind | F25 思考节律 |
| 语义搜索 | AI 全量排序（前端+API） | F27 语义搜索，无 embedding 存储 |

### 目录结构

```
spark/
├── apps/web/               # Next.js 主应用
│   └── src/app/
│       ├── page.tsx         # 首页（想法流 + 杂交台 + 连接器）
│       ├── ideas/[id]/     # 想法详情页
│       ├── graveyard/      # 想法墓地
│       ├── settings/       # 设置页
│       ├── graph/          # 想法图谱
│       ├── retro/          # 回顾中心
│       ├── kanban/         # 看板视图 ✅
│       ├── explore/        # 语义搜索与发现 ✅
│       ├── rhythm/         # 思考节律 + 盲区地图 ✅
│       ├── profile/        # 思考风格画像 ✅
│       └── api/            # API 路由
├── packages/@spark/
│   ├── ai/                 # AI 思考伙伴
│   │   └── src/
│   │       ├── provider.ts  # Provider 抽象 ✅
│   │       ├── socratic.ts  # 反应层 ✅
│   │       ├── connector.ts # 主动层 ✅
│   │       ├── catalyst.ts  # 主动层 ✅
│   │       ├── retro.ts     # 反应层 ✅
│   │       ├── mirror.ts    # 反思层 🚧
│   │       ├── devil.ts     # 主动层 🚧
│   │       ├── coroner.ts   # 反思层 🚧
│   │       ├── translator.ts # 主动层 🚧
│   │       ├── blindspot.ts # 反思层 ✅
│   │       ├── profile.ts   # 反思层 ✅
│   │       └── genetics.ts  # 主动层 📋
│   ├── db/                # 数据层
│   └── utils/             # 工具函数
```

### AI Provider 配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 端点 | OpenAI 兼容接口 | 可替换 |
| 模型 | DeepSeek 系列 | 推理模型 |
| max_tokens 默认 | 4000 | provider.ts 默认值 |
| max_tokens 复杂任务 | 8000 | connector/catalyst/mirror 等需 JSON 输出的场景 |
| extractJson 工具 | ✅ | 剥离 markdown 代码围栏后解析 JSON |

---

## 十五、设计规范

### UI 设计

- 遵循 shadcn/ui 默认主题，Tailwind neutral 色板
- 简洁、留白充足、文字为主
- 避免装饰性元素（渐变、插图、阴影堆叠）
- 响应式：桌面优先，移动端适配
- 卡片圆角 10px
- 中文字体使用系统默认字体栈

### 交互设计

- 筛选采用「透镜 + 筛选弹层 + 激活条件标签」模式
- 平时只展示透镜行，避免筛选区铺满占用主层级
- 筛选/排序变更时保留已有列表，静默替换，避免列表晃动
- 卡片颜色在不同视图下保持一致，不因筛选状态变浅
- AI 功能按需触发，不做 always-on 推送
- 输入框支持 ⌘+↵ 快速发送
- 看板箭头按钮移动状态，不拖拽（移动端友好）
- 重要程度使用色点+文字标签，0 不显示避免噪音
- 集合使用哈希着色（8 色调色板），同集合颜色一致

### AI 设计

- AI 是思考伙伴，不是思考替代
- AI 是按需邀请的，默认关闭
- AI 只负责"发现并提议"，确认永远由用户做
- 用户对数据有完全控制权
- API 调用失败时静默降级，不影响核心功能
- 反思层 AI 洞察不推送通知，用户自然访问时看到
- 数据发送分三层敏感度：完整内容（苏格拉底/连接器/杂交台/反方辩手/跨界翻译）、元数据统计（思考镜子/风格画像/盲区地图）、已放弃想法+墓志铭（验尸官）

---

## 十六、成功指标

### 定量指标

| 指标 | 当前 | 目标 | 衡量方式 |
|------|------|------|----------|
| 想法关联率 | 0% | >30% | idea_relationships 行数 / ideas 总数 |
| idea 状态前进率 | 未知 | >40% | status_change 活动统计 |
| 周活跃回看次数 | ~0 | >3 次/周 | 页面访问 + 闪卡点击 |
| AI 功能使用率 | N/A | >50% 周活 | AI 交互日志 |
| AI 连接确认率 | N/A | >20% | 用户确认 / AI 推荐 |
| AI 苏格拉底回答率 | N/A | >40% | 回答 / 总追问 |
| 看板使用率 | N/A | >30% 想法通过看板推进 | kanban 状态变更活动 |
| 思考节律访问 | N/A | >2 次/月 | /rhythm 页面 PV |
| 盲区检测触发率 | N/A | >20% 月活 | /api/ai/blindspot 调用次数 |
| 风格画像生成率 | N/A | >20% 月活 | /api/ai/profile 调用次数 |

### 成功标准

1. AI 建议的连接中，用户确认率 >20%
2. AI 苏格拉底问题被回答（而非跳过）的比例 >40%
3. AI 功能的"打扰感"评分 <3/10
4. 零用户反馈"AI 替我思考了"
5. 看板帮助 >30% 的想法从种子推进到萌芽+
6. 盲区检测触发后，>20% 用户在两周内写出盲区主题的想法
7. 思考镜子洞察被用户认为"有启发"的比例 >50%（待 UI 集成后衡量）

---

## 十七、边界情况与降级策略

| 场景 | 处理方式 |
|------|----------|
| AI API 不可用 | 所有 AI 功能静默降级，不报错不阻塞核心功能 |
| AI 返回非法 JSON | 清除 markdown 代码围栏后重试解析，仍失败则降级为空结果 |
| 数据库连接失败 | 显示错误提示，已输入内容不丢失 |
| 空数据状态 | 每个视图有友好空状态文案 |
| 胶囊未解锁 | 列表显示锁图标 + 倒计时，详情页隐藏正文和时间线和血脉 |
| 想法数量不足 | AI 连接器 >= 5 条，杂交台 >= 4 条，思考镜子 >= 20 条，验尸官 >= 5 条，盲区/画像 >= 15 条 |
| 搜索无结果 | 显示"没有找到匹配的想法" |
| 大量数据 | 分页加载，每页 20 条；图谱 >100 节点切换布局优化 |
| 并发编辑 | 乐观更新，冲突时提示刷新 |
| 健康度计算 | 📋 规划中：新想法默认 50 分（活跃），无活动 30 天降至枯萎区 |

---

## 十八、功能依赖关系

```
F01 闪电捕获 -- 基础，所有功能的前置
  |-- F02 想法卡片列表
  |   |-- F03 重要程度
  |   |-- F05 基础搜索
  |   `-- F22 快速操作
  |-- F04 想法详情页
  |   |-- F08 想法墓志铭
  |   |-- F09 想法墓地 -- F32 想法验尸官（P4） 🚧
  |   |-- F17 关联手动建立
  |   |-- F18 情绪标记
  |   |-- F20 想法链接语法
  |   |-- F24 想法血脉（P3） 📋
  |   |-- F28 想法健康度（P4） 📋
  |   |-- F31 AI 反方辩手（P4） 🚧
  |   `-- F33 AI 跨界翻译（P4） 🚧
  |-- F06 AI 苏格拉底
  |-- F07 时间胶囊
  |   `-- F13 AI 回望对话
  |-- F11 AI 连接器
  |   `-- F15 想法图谱
  |-- F12 想法杂交台
  |   `-- F35 想法遗传与变异（P4） 📋
  |-- F16 回访系统
  |-- F25 思考节律（P3） ✅
  |   |-- F29 思考风格画像（P4） ✅
  |   |-- F30 灵感生态位（P4） 📋
  |   `-- F34 思维盲区地图（P4） ✅
  |-- F26 看板视图（P3） ✅
  |-- F27 语义搜索（P3） ✅
  `-- F10/F14 AI 隐私控制（控制所有 AI 功能）

P3 依赖：
  F23 AI 思考镜子 🚧 <- >= 20 条想法 + 30 天数据（API 就绪）
  F25 思考节律 ✅   <- 元数据积累
  F26 看板视图 ✅    <- 状态变更 API
  F27 语义搜索 ✅   <- AI 排序（无 embedding 存储）

P4 依赖：
  F28 健康度 📋     <- 依赖活动数据 + 关联数据
  F29 风格画像 ✅    <- 依赖 F25 思考节律数据
  F32 验尸官 🚧      <- 依赖 F09 想法墓地 + >= 5 条归档（API 就绪）
  F34 盲区地图 ✅    <- 依赖 >= 15 条想法
  F35 遗传变异 📋   <- 依赖 F12 杂交台 + parent_a/b_id
```

---

## 十九、迭代策略

| 原则 | 说明 |
|------|------|
| 版本独立可用 | V1 完全不需要 AI；V2 AI 是可选增强；V3+ AI 洞察是自然延伸 |
| 功能灰度 | AI 功能通过设置开关控制，可随时关闭单个功能 |
| 向后兼容 | schema 变更只加字段不删字段，新增字段都有默认值 |
| 文档同步 | 每次变更对照 git diff 检查文档同步（AGENTS.md 硬规则） |
| 发布节奏 | 个人项目，按功能模块迭代，不设固定周期 |
| 验证驱动 | 每个版本验证一个核心假设 |

### 版本验证假设

| 版本 | 核心假设 | 验证方式 |
|------|----------|----------|
| V1 | AI 追问有价值吗？ | 苏格拉底回答率 >40% |
| V2 (AI) | AI 能发现用户看不到的连接吗？ | 连接确认率 >20% |
| V2 (非功能) | 可视化和回访能对抗遗忘吗？ | 周活回看 >3 次 |
| V3 | 可视化+AI 能帮用户看清自己的思考模式吗？ | 思考节律访问 + 盲区检测触发率 + 风格画像生成率 |
| V4 | 健康度和盲区检测能改善决策吗？ | 健康度预警后推进/安葬率 >30%（待 F28 实现） |

---

## 二十、版本路线图

### V1 — "种子"（MVP，已完成 ✅）

- ✅ 闪电捕获（纯文本 + Markdown）
- ✅ 想法卡片列表（透镜筛选 + 时间线视图 + 分页）
- ✅ 重要程度（0-4 五级 + 自定义名称 + 色彩体系）
- ✅ 单个想法详情页（状态 + 时间线 + 情绪标记）
- ✅ 基础搜索
- ✅ MySQL 存储
- ✅ AI 苏格拉底（追问一个问题的最小验证）
- ✅ 时间胶囊（密封 + 解锁）
- ✅ 想法墓志铭（归档原因 + 墓地页面）
- ✅ AI 总开关 + 隐私说明

### V2 — "萌芽"（已完成 ✅）

**AI 思考伙伴：**
- ✅ AI 连接器（主动发现想法间连接）
- ✅ 想法杂交台（AI 挑选碰撞对）
- ✅ AI 回望对话（胶囊解锁后对话）
- ✅ AI 隐私控制完整版（分功能开关 + 数据日志）

**可视化与体验增强：**
- ✅ 想法图谱（D3.js 力导向图）
- ✅ 回访系统（每日闪卡 + 周报 + 遗忘预警）
- ✅ 关联手动建立
- ✅ 情绪标记
- ✅ 想法集合管理
- ✅ 想法链接 [[]] 语法
- ✅ 想法老化提示
- ✅ 快速操作

### V3 — "生长"（进行中）

**P3 功能：**
- ✅ 思考节律（F25）— 捕获热力图 + 状态前进率 + 重要程度/状态/情绪分布 + 活动趋势
- ✅ 看板视图（F26）— 4 列行动看板，箭头移动状态
- ✅ 语义搜索（F27）— AI 语义排序，双模式搜索
- ✅ 思维盲区地图（F34）— 集成在 /rhythm 页面
- ✅ 思考风格画像（F29）— /profile 页面 + AI 叙事性画像
- 🚧 AI 思考镜子（F23）— API 就绪，retro 页 UI 集成待完成
- 📋 想法血脉（F24）— 演化路径可视化（API 待开发）

**验证假设**：可视化+AI 能帮用户看清自己的思考模式吗？

### V4 — "花园"

**P4 功能：**
- 🚧 AI 反方辩手（F31）— API 就绪，详情页 UI 集成待完成
- 🚧 AI 想法验尸官（F32）— API 就绪，墓地页 UI 集成待完成
- 🚧 AI 跨界翻译（F33）— API 就绪，详情页 UI 集成待完成
- 📋 想法健康度系统（F28）— 综合活力分数 + 预警（待开发）
- 📋 灵感生态位（F30）— 径向布局可视化思考版图（待开发）
- 📋 想法遗传与变异（F35）— 杂交后代继承 + 变异（待开发）

**验证假设**：健康度和盲区检测能改善想法决策吗？

### V5+ — 远期

- 季度回顾
- 导出 / 备份（Markdown / JSON）
- 主题自定义
- 多端入口（浏览器扩展、快捷指令、Telegram Bot）
- 本地模型支持（Ollama，完全离线 AI）
- 向量数据库（>500 条想法后）
- AI 功能开关扩展至 10 功能（mirror/devil/coroner/translate/blindspot/profile）
- 拖拽看板（V5 桌面端增强）

---

## 二十一、开放问题

| 编号 | 问题 | 状态 | 决策 |
|------|------|------|------|
| Q01 | AI Provider 选择 | ✅ 已定 | DeepSeek（OpenAI 兼容，可替换） |
| Q02 | 语义搜索实现 | ✅ 已定 | V3 用 AI 全量排序，无 embedding 存储；>500 条后考虑向量数据库 |
| Q03 | AI 苏格拉底触发条件 | ✅ 已定 | 非时间胶囊即触发，不设 importance 门槛 |
| Q04 | 杂交台频率 | ✅ 已定 | 用户手动触发 |
| Q05 | 胶囊解锁后是否变回普通 idea | ✅ 已定 | 保留标记，可正常查看 |
| Q06 | 情绪标记关联墓志铭 | 📋 待定 | V4 再考虑 |
| Q07 | AI 连接器 embedding 是否离线 | 📋 规划中 | V2 发送 API，V5 支持本地 |
| Q08 | 思考镜子导出 | 📋 待定 | V5 和导出一起做 |
| Q09 | [[]] 链接语法自动建关联 | 📋 待定 | 建议自动建立 related 类型，用户可删除 |
| Q10 | 图谱大数据量降级 | 📋 待定 | >100 节点时切换布局优化模式 |
| Q11 | 健康度计算频率 | 📋 待定 | 实时计算 vs 定时批量计算 |
| Q12 | 盲区地图的分类标准 | 📋 待定 | 用 AI 生成分类 vs 预设分类体系 |
| Q13 | 反方辩手的语气 | 📋 待定 | 严格学术风 vs 友好对话风 |
| Q14 | 遗传变异的变异方向 | 📋 待定 | AI 建议 vs 随机生成 |
| Q15 | 看板是否支持拖拽 | ✅ 已定 | V3 用箭头按钮（移动端友好），V5 桌面端考虑拖拽 |
| Q16 | AI 功能开关数量 | 📋 待定 | 当前 4 功能开关，需扩展至 10 功能 |

---

*文档版本：v5.0 · 2026-08-18 · 状态：Active*
*变更记录：v4.0→v5.0 — P3/P4 功能实现状态与代码同步：F25/F26/F27/F29/F34 标记为已实现并补充详细规格；F23/F31/F32/F33 标记为 API 就绪待 UI 集成；更新 API 清单（新增 6 个 AI 端点）、AI Provider 架构、信息架构（新增 4 个页面路由）、AI 功能开关矩阵；整合设计规范决策（筛选 UX、卡片颜色一致性、列表刷新策略、重要程度色彩体系、集合哈希着色）；更新成功指标和迭代策略*
