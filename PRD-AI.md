# Spark AI 助手 PRD — "思考伙伴"

> 一份让 AI 成为思考伙伴而非思考替代的产品规格。

---

## 1. 元数据

| 字段 | 内容 |
|------|------|
| 项目名称 | Spark — 想法操作系统 · AI 助手模块 |
| 产品经理 | Codex (Senior PM) |
| 版本 | v1.1 |
| 日期 | 2026-08-18 |
| 状态 | Active（P0+P1 已实现；v1.1 修复苏格拉底触发条件与 PRD.md v3.0 对齐） |
| 关联文档 | PRODUCT.md / SPEC.md |

---

## 2. 背景与目标

### 问题陈述

Spark 已经能帮用户捕获、标记、追踪想法，但想法从"写下"到"实现"之间的催化力量完全依赖用户自觉——没有人帮你看清自己看不到的连接，没有人在你敷衍时追问一句，没有人在你遗忘时把旧想法重新推到眼前。

### 现状

- V1 已完成：闪电捕获、想法卡片列表、重要程度（0-4 五级 + 自定义名称）、详情页（状态 + 时间线）、基础搜索、透镜筛选
- V2 规划中：想法图谱、回访系统、关联手动建立、情绪标记
- 用户行为假设：用户会持续捕获想法，但回看频率低，想法之间几乎不主动建立连接，大部分 idea 长期停留在 seed 状态
- 数据基础：每条 idea 有 title、content、status、importance、collection、created_at、updated_at、活动时间线，足以支撑 AI 洞察

### 目标

**定性目标**：让 AI 成为用户的"思考伙伴"——主动发现连接、用提问逼用户走深一步、在遗忘时点亮旧想法、用用户自己的数据生成自我认知——但永远不替代用户思考。

**定量目标**：

| 指标 | 当前 | 目标 | 衡量方式 |
|------|------|------|----------|
| 想法关联率（有连接的 idea 占比） | 0% | >30% | idea_relationships 表行数 / ideas 总数 |
| idea 状态前进率（seed -> sprout+ 的比例） | 未知 | >40% | status_change 活动记录统计 |
| 周活跃回看次数 | ~0（仅每日闪卡） | >3 次/周 | retro 页面访问 + 闪卡点击 |
| AI 功能使用率（启用 AI 的用户中） | N/A | >50% 周活使用至少 1 次 | AI 交互日志 |

### 成功标准

1. 用户在 AI 建议的连接中，确认率 >20%（说明建议有质量，不是噪音）
2. AI 苏格拉底问题被回答（而非跳过）的比例 >40%
3. AI 功能的"打扰感"评分 <3/10（用户主观调查，低打扰）
4. 零用户反馈"AI 替我思考了"（保持思考主权）

### 设计原则

AI 在 Spark 中的角色边界：

1. **AI 是思考伙伴，不是思考替代** — 不替用户生成想法内容、不替用户分类、不替用户写反思
2. **AI 是按需邀请的，不是默认开启的** — 每次交互需用户主动触发，设置页有总开关，默认关闭
3. **AI 只负责"发现并提议"，确认永远由用户做** — 不自动建边、不自动改状态、不自动归档
4. **用户对数据有完全控制权** — 开启 AI 时明确告知数据将发送到外部服务，可随时关闭

### 红线（AI 绝对不做的事）

- 不替用户生成或润色想法内容
- 不在捕获时自动分类/打标签
- 不自动写墓志铭或胶囊回信
- 不做 always-on 的 AI 聊天界面

---

## 3. 用户画像

### 目标用户

| 维度 | 描述 |
|------|------|
| 角色 | 创作者 / 独立开发者 / 产品人 / 内容创作者 |
| 身份 | 唯一用户（Spark 是个人工具） |
| 使用场景 | 灵感闪现时快速捕获；定期回看孵化想法；在想法间寻找连接和碰撞 |
| 核心目标 | 把碎片灵感发展成可执行的想法，最终变成现实 |
| 痛点 | 想法存了就忘；看不出想法之间的联系；写到一半就搁置；不知道自己的思考模式 |
| 技术熟练度 | 中高（能使用本地 Docker、理解 Markdown） |
| AI 接受度 | 中等——欢迎 AI 辅助思考，但反感 AI 替代思考 |

### 非目标用户

- 团队协作用户（Spark 不做多用户协作）
- 纯笔记用户（如果只是存笔记，Notion/Obsidian 更合适）
- AI 重度依赖用户（期望 AI 直接生成内容而非引导思考的人）

---

## 4. 用户故事

### Epic 1: AI 苏格拉底（Socratic Questioner）

```
作为创作者
我想要 在保存想法后被追问一个有深度的问题
以便 被迫走深一步，而不是把粗糙的灵感搁置了事
```

**验收标准：**

- [ ] 保存 idea 后（非时间胶囊），系统显示一个 AI 生成的追问
- [ ] 问题只显示一个，不连续追问
- [ ] 用户可以回答（答案存为 note 类型活动）或跳过（点击"下次再说"）
- [ ] 用户可以在设置中关闭此功能
- [ ] 如果 API 调用失败，静默降级——不显示问题，不报错，不阻塞保存
- [ ] 问题语言与 idea 内容语言一致

### Epic 2: AI 连接器（Connection Maker）

```
作为创作者
我想要 AI 主动发现我没想到的想法之间的连接
以便 看到自己的思维盲区，发现潜在的交叉点
```

**验收标准：**

- [ ] 用户在首页或图谱页可以点击"发现连接"按钮主动触发
- [ ] AI 读取所有 idea 的 title + content，用 embedding 计算语义距离
- [ ] 系统推荐 1-3 对"你可能没注意到的连接"，每对附一句解释
- [ ] 用户可以确认关联（建立 idea_relationships 记录）或忽略
- [ ] AI 不自动建边，所有关联需用户确认
- [ ] 已有关联的 idea 对不再重复推荐
- [ ] 如果 idea 总数 <5，提示"想法还不够多，多写几条再来发现连接"

### Epic 3: 想法杂交台 + AI 催化剂（Hybrid Catalyst）

```
作为创作者
我想要 AI 挑选两个看似不相关但底层结构相似的想法放在一起
以便 通过碰撞产生我靠自己想不到的新灵感
```

**验收标准：**

- [ ] 用户在首页看到"杂交台"区域，显示一对 AI 挑选的 idea
- [ ] 两个 idea 来自不同集合或不同主题领域
- [ ] AI 附一句提示，说明为什么这两个想法可能碰撞出新东西
- [ ] 用户可以写下碰撞灵感（直接生成一条新 idea，标记两个"亲本"）
- [ ] 用户可以跳过当前配对，换一对新的
- [ ] 杂交台每天/每周刷新一次，不做 always-on 推送
- [ ] 新 idea 的活动时间线自动记录"从「idea A」和「idea B」杂交诞生"

### Epic 4: 时间胶囊 + AI 回望对话（Retro Dialogue）

```
作为创作者
我想要 写下一个想法密封起来，到时间后和了解我完整上下文的 AI 对话回望
以便 与过去的自己对话，看到自己的思考如何变化
```

**验收标准：**

- [ ] 用户创建 idea 时可以标记为"时间胶囊"，设定解锁日期（30天/90天/1年/自定义）
- [ ] 胶囊在解锁前不可查看内容（列表中只显示标题和倒计时）
- [ ] 解锁后，AI 读取该 idea 的原始内容、当时的情绪标记、中间的活动记录，生成回望对话
- [ ] AI 用提问引导用户反思，如"你当时说的是 X，现在怎么看？"
- [ ] 用户可以对话多轮，每轮 AI 都基于完整上下文
- [ ] 对话结束后，用户可以写一句"现在的我怎么看"存入活动时间线
- [ ] AI 不替用户写反思总结

### Epic 5: 想法墓志铭（Idea Epitaph）

```
作为创作者
我想要 在放弃一个想法时写一句墓志铭
以便 记录放弃的原因，日后复盘自己的决策模式
```

**验收标准：**

- [ ] idea 状态变更为 archived 或 dormant 时，弹出轻量提示（可跳过）
- [ ] 用户可以写一句"为什么放弃"（墓志铭）
- [ ] 墓志铭存为 general 类型的活动记录
- [ ] 设置页有"想法墓地"入口，展示所有已归档/休眠 idea 的墓志铭
- [ ] 墓志铭是可选的，不强制，不阻塞状态变更

### Epic 6: AI 思考镜子（Thinking Mirror）

```
作为创作者
我想要 AI 用我自己的数据生成关于我思考模式的洞察
以便 发现自己没意识到的行为模式，改善思考习惯
```

**验收标准：**

- [ ] 每月一次，AI 读取用户所有 idea 的元数据（不含完整内容，只读统计）
- [ ] 生成 3-5 条叙事性洞察，如"你的'必做'标签实现率是 0%"
- [ ] 洞察用自然语言叙述，不用图表和数字堆砌
- [ ] 每条洞察附带一个可操作建议（可选采纳）
- [ ] 洞察放在 retro 页面，不推送通知
- [ ] 需要至少 20 条 idea 和 30 天使用数据才能生成

### Epic 7: 想法血脉（Idea Lineage）

```
作为创作者
我想要 看到一个想法从碎片到实现的完整演化路径
以便 理解自己的思考过程，复制成功的路径
```

**验收标准：**

- [ ] 详情页展示"血脉"视图（时间轴形式）
- [ ] 血脉包含：原始捕获 -> 状态变更 -> 关键活动 -> 关联建立 -> 杂交后代 -> 最终状态
- [ ] 杂交产生的 idea 自动显示亲本链接
- [ ] 手动标记为"衍生自"的关联也纳入血脉
- [ ] 血脉视图可折叠，默认不展开（避免噪音）

---

## 5. 功能需求

### P0 — Must Have（V1 尾 / V2 早）✅ 已实现

| 编号 | 功能 | 描述 | Epic | 预估 Effort |
|------|------|------|------|-------------|
| F01 | AI 总开关 | 设置页新增 AI 助手开关，默认关闭；开启时显示隐私说明 | 全局 | 0.5d |
| F02 | AI 苏格拉底 | 保存非胶囊 idea 后生成一个追问，可答可跳 | E1 | 2d |
| F03 | 时间胶囊 | idea 标记为胶囊 + 解锁日期；解锁前隐藏内容；解锁后可查看 | E4 | 1d |
| F04 | 想法墓志铭 | 状态变更为 archived/dormant 时弹墓志铭输入；想法墓地页面 | E5 | 1.5d |

### P1 — Should Have（V2）✅ 已实现

| 编号 | 功能 | 描述 | Epic | 预估 Effort |
|------|------|------|------|-------------|
| F05 | AI 连接器 | "发现连接"按钮 + embedding 计算 + LLM 解释 + 用户确认建边 | E2 | 5d |
| F06 | 想法杂交台 | 首页杂交台区域 + AI 挑选配对 + 碰撞生成新 idea | E3 | 3d |
| F07 | AI 回望对话 | 胶囊解锁后的多轮对话 + 上下文注入 + 反思存档 | E4 | 2d |
| F08 | AI 隐私控制 | 设置页可选择哪些 AI 功能开启；显示数据发送日志 | 全局 | 1d |

### P2 — Could Have（V3）

| 编号 | 功能 | 描述 | Epic | 预估 Effort |
|------|------|------|------|-------------|
| F09 | AI 思考镜子 | 每月叙事性洞察生成 + retro 页面展示 | E6 | 4d |
| F10 | 想法血脉 | 详情页血脉视图 + 杂交亲本链 + 衍生关联 | E7 | 5d |
| F11 | 思考节律 | 元数据统计面板（捕获时段、状态前进率、标签实现率） | E6 | 3d |

### Won't Have（本期明确不做）

- AI 自动生成想法内容
- AI 自动分类 / 打标签
- Always-on AI 聊天界面
- AI 代写墓志铭 / 胶囊回信 / 反思总结
- 多用户 AI 协作

---

## 6. 非功能需求

### 性能

| 场景 | 指标 |
|------|------|
| AI 苏格拉底问题生成 | < 3s（用户保存后等待，需 loading 态） |
| AI 连接器计算 | < 10s（可异步，结果推送） |
| AI 回望对话响应 | < 5s / 轮 |
| AI 思考镜子生成 | < 30s（月度任务，可后台跑） |
| Embedding 计算 | < 5s（100 条 idea 以内） |

### 隐私与安全

- AI 功能默认关闭，开启时显示明确的数据发送说明
- 每次 AI 交互都是用户主动触发，不自动扫描不自动发送
- 设置页显示"AI 数据发送日志"，记录每次 API 调用的时间、功能、数据量
- API Key 存储在本地环境变量，不暴露到前端
- 长期目标：支持本地模型（Ollama），实现完全离线的 AI 功能

### 可用性

- AI API 调用失败时静默降级，不影响核心功能
- AI 功能不可用时，所有非 AI 功能正常运作
- 网络断开时，AI 功能不可用但不报错

### 可维护性

- AI 相关代码集中在 packages/@spark/ai/ 包内
- AI Provider 可替换（接口抽象，支持 OpenAI / Anthropic / 本地模型）
- AI 交互日志独立存储，便于审计和调试

---

## 7. 交互流程

### 7.1 AI 苏格拉底

```
用户输入标题 + 内容 -> 点击保存
  -> 系统保存 idea（不阻塞）
  -> 如果非时间胶囊且 AI 开启
    -> 调用 AI 生成追问（loading: "正在思考一个问题..."）
    -> 显示问题卡片（可折叠，不打断列表浏览）
    -> 用户回答 -> 存为 note 活动
    -> 用户跳过 -> 问题消失，不存储
  -> 如果 AI 关闭或调用失败
    -> 静默跳过，无感知
```

### 7.2 AI 连接器

```
用户点击"发现连接"按钮
  -> 如果 idea 总数 < 5 -> 提示"多写几条再来"
  -> 否则：
    -> 计算所有 idea 的 embedding（首次全量，后续增量）
    -> 找出语义距离适中（不相似也不完全不相关）的 idea 对
    -> LLM 生成解释："这两个想法可能在讨论..."
    -> 展示 1-3 对建议（卡片形式）
    -> 每对可操作：确认关联 / 忽略 / 查看详情
    -> 确认关联 -> 写入 idea_relationships 表
```

### 7.3 想法杂交台

```
首页加载时（或用户点击"换一对"）
  -> AI 从不同集合/主题中挑选 2 个 idea
  -> 选择标准：语义不相似但底层结构相似（embedding 距离中等 + LLM 判断）
  -> 展示杂交台卡片：
    [idea A 标题 + 摘要]  X  [idea B 标题 + 摘要]
    AI 提示："这两个都在解决..."
  -> 用户操作：
    -> 写碰撞灵感 -> 生成新 idea（标记 parent_a, parent_b）
    -> 换一对 -> 重新选择
    -> 跳过 -> 杂交台收起，下次刷新再出现
```

### 7.4 时间胶囊 + AI 回望

```
创建 idea 时 -> 勾选"设为时间胶囊" -> 选择解锁日期
  -> idea 保存，列表中显示锁图标 + 倒计时
  -> 详情页在解锁前只显示标题 + 倒计时 + "将在 X 天后解锁"

解锁日到达
  -> idea 自动变为可查看
  -> 首次打开时提示"你的时间胶囊解锁了"
  -> 如果 AI 开启：
    -> AI 读取原始内容 + 中间活动 + 情绪标记
    -> 生成回望对话开场白
    -> 用户可多轮对话
    -> 对话结束 -> 用户写"现在的我怎么看" -> 存入活动
  -> 如果 AI 关闭：
    -> 仅展示原始内容 + 时间线，用户自行回顾
```

### 7.5 想法墓志铭

```
用户将 idea 状态改为 archived 或 dormant
  -> 状态变更成功（不阻塞）
  -> 弹出轻量提示（非模态，可忽略）：
    "为什么放弃这个想法？（可选）"
    [输入框]  [跳过]
  -> 用户输入 -> 存为 general 活动："墓志铭：{内容}"
  -> 用户跳过 -> 不存储，不追问

想法墓地页面（/graveyard 或 retro 子页）
  -> 展示所有 archived + dormant 的 idea
  -> 每条显示：标题 + 状态 + 墓志铭（如有）+ 归档时间
  -> 按归档时间倒序
```

### 7.6 AI 思考镜子

```
每月 1 号（或用户手动触发）
  -> 检查数据量：idea >= 20 且使用 >= 30 天
  -> 读取元数据（不读完整内容）：
    - 每条 idea 的 importance, status, created_at, updated_at
    - 状态变更历史
    - 活动频率
  -> AI 生成 3-5 条叙事性洞察
  -> 展示在 retro 页面的"思考镜子"板块
  -> 不推送通知，用户自然访问时看到
```

---

## 8. 数据模型变更

### ideas 表新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| is_capsule | BOOLEAN DEFAULT FALSE | 是否为时间胶囊 |
| unlock_at | DATETIME(3) NULL | 胶囊解锁时间 |
| parent_a_id | VARCHAR(36) NULL | 杂交亲本 A（FK -> ideas.id） |
| parent_b_id | VARCHAR(36) NULL | 杂交亲本 B（FK -> ideas.id） |
| epitaph | TEXT NULL | 墓志铭（归档时填写） |

### 新增 idea_relationships 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | VARCHAR(36) | PK | 唯一标识 |
| source_id | VARCHAR(36) | FK -> ideas.id | 关联来源 idea |
| target_id | VARCHAR(36) | FK -> ideas.id | 关联目标 idea |
| type | VARCHAR(20) | NOT NULL | 关联类型：related / conflict / derived / parent_child |
| created_by | VARCHAR(10) | NOT NULL DEFAULT 'user' | 建立者：user / ai_suggested |
| ai_explanation | TEXT NULL | AI 推荐时的解释文本 |
| created_at | DATETIME(3) | NOT NULL | 建立时间 |

### 新增 ai_interactions 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | VARCHAR(36) | PK | 唯一标识 |
| feature | VARCHAR(30) | NOT NULL | 功能：socratic / connector / catalyst / retro / mirror |
| idea_id | VARCHAR(36) NULL | FK -> ideas.id | 关联的 idea（如有） |
| request_summary | VARCHAR(200) NULL | 请求摘要（不含完整内容） |
| response_summary | VARCHAR(500) NULL | 响应摘要 |
| tokens_used | INT NULL | token 消耗 |
| created_at | DATETIME(3) | NOT NULL | 交互时间 |

### settings 表新增键

| 键 | 值格式 | 说明 |
|------|--------|------|
| ai_enabled | boolean | AI 总开关 |
| ai_features | JSON | 各功能开关：{ socratic: true, connector: true, ... } |
| ai_provider | string | AI 提供商：openai / anthropic / local |
| ai_model | string | 模型名称 |

---

## 9. 开放问题

| 编号 | 问题 | 影响 | 建议 | 决策者 |
|------|------|------|------|--------|
| Q01 | AI Provider 选择：OpenAI / Anthropic / 本地模型？ | 成本、隐私、质量 | V2 先用 OpenAI（生态成熟），V3 支持本地模型 | 用户 |
| Q02 | Embedding 存储：MySQL vs 向量数据库？ | 性能、复杂度 | V2 先存 MySQL（JSON 字段），>500 条 idea 后再考虑 sqlite-vec 或外部 | 开发者 |
| Q03 | AI 苏格拉底的触发条件 | 打扰频率 | 已定：非时间胶囊即触发，不设 importance 门槛 | PM |
| Q04 | 杂交台频率：每天 vs 每周 vs 用户手动？ | 新鲜感 vs 打扰 | 默认每天一次，可设置中调整 | 用户 |
| Q05 | 时间胶囊解锁后是否自动变回普通 idea？ | 数据模型复杂度 | 解锁后保留胶囊标记但可正常查看和编辑 | PM |
| Q06 | 墓志铭是否需要情绪标记关联？ | 功能耦合度 | V1 不关联，保持独立 | PM |
| Q07 | AI 连接器的 embedding 是否需要离线计算？ | 隐私 | V2 发送到 API，V3 支持本地 embedding | 开发者 |
| Q08 | 思考镜子是否需要导出？ | 数据可移植性 | V3 和导出功能一起做 | PM |

---

## 10. 版本规划

### Phase 1 — V1 尾声（"种子"收尾）✅ 已完成

**目标**：验证"AI 在 Spark 中有没有感觉"

| 功能 | 说明 |
|------|------|
| F01 AI 总开关 | 设置页开关 + 隐私说明 |
| F02 AI 苏格拉底 | 保存后追问一个问题的最小可用版本 |
| F03 时间胶囊 | 无 AI 版本，纯密封 + 解锁 |
| F04 想法墓志铭 | 无 AI 版本，纯墓志铭 + 墓地页面 |

**验证假设**：用户是否觉得 AI 追问有价值（回答率 >40%）

### Phase 2 — V2（"萌芽"扩展）✅ 已完成（AI 模块）

**目标**：AI 让想法之间的连接从被动变主动

| 功能 | 说明 |
|------|------|
| F05 AI 连接器 | 主动发现 + 用户确认建边 |
| F06 想法杂交台 | AI 挑选 + 碰撞生成新 idea |
| F07 AI 回望对话 | 胶囊解锁后的 AI 对话 |
| F08 AI 隐私控制 | 分功能开关 + 数据发送日志 |

### Phase 3 — V3（"生长"阶段）

**目标**：AI 帮用户看清自己的思考模式

| 功能 | 说明 |
|------|------|
| F09 AI 思考镜子 | 月度叙事性洞察 |
| F10 想法血脉 | 演化路径可视化 |
| F11 思考节律 | 元数据统计面板 |

---

## 11. 附录

### A. RICE 评分明细

| 功能 | Reach | Impact | Confidence | Effort | RICE | 阶段 |
|------|-------|--------|------------|--------|------|------|
| AI 苏格拉底 | 9 | 6 | 80% | 2d | 21.6 | P0 |
| AI 连接器 | 10 | 8 | 70% | 5d | 11.2 | P1 |
| 想法杂交台 | 8 | 8 | 65% | 3d | 13.9 | P1 |
| AI 回望对话 | 6 | 7 | 70% | 2d | 14.7 | P1 |
| 想法墓志铭 | 8 | 6 | 85% | 1.5d | 27.2 | P0 |
| 时间胶囊 | 10 | 7 | 90% | 1d | 63.0 | P0 |
| AI 思考镜子 | 7 | 7 | 75% | 4d | 9.2 | P2 |
| 想法血脉 | 6 | 7 | 70% | 5d | 5.9 | P2 |
| 思考节律 | 7 | 5 | 80% | 3d | 9.3 | P2 |

### B. KANO 分类

| 功能 | 分类 | 理由 |
|------|------|------|
| AI 总开关 | 基本型 | 没有 AI 开关，无法信任使用 AI 功能 |
| AI 苏格拉底 | 兴奋型 | 用户没有预期被追问，有了会惊喜 |
| AI 连接器 | 兴奋型 | 主动发现连接超出预期 |
| 时间胶囊 | 兴奋型 | 情感化功能，超出工具预期 |
| 想法墓志铭 | 期望型 | 归档时有交代是合理的期望 |
| AI 回望对话 | 兴奋型 | 与过去的自己对话超出预期 |
| AI 思考镜子 | 期望型 | 了解自己是深层期望 |
| 想法血脉 | 期望型 | 追溯演化路径是合理期望 |

### C. 竞品对比

| 维度 | Spark | Notion AI | Mem.ai | Obsidian + AI 插件 |
|------|-------|-----------|-------|-------------------|
| 核心定位 | 想法生长系统 | 文档协作 | AI 笔记 | 知识库 |
| AI 角色 | 思考伙伴（追问、发现连接） | 内容生成（写、总结、翻译） | 自动关联 + 聊天 | 插件依赖，无统一角色 |
| AI 替代思考？ | 明确不替代 | 部分替代（生成内容） | 大量替代（AI 写笔记） | 因插件而异 |
| 主动发现连接 | AI 连接器（核心功能） | 无 | 自动关联（相似度） | 无（需手动） |
| 想法杂交 | AI 催化剂（独有） | 无 | 无 | 无 |
| 时间维度 | 胶囊 + 回望 + 血脉 | 无 | 无 | 无 |
| 思考模式洞察 | AI 思考镜子 | 无 | 无 | 无 |
| 数据隐私 | 本地优先 + 按需发送 | 云端 | 云端 | 本地 + 插件依赖 |
| 差异化 | 想法生命周期 + AI 催化交叉点 | 通用文档 AI | AI 代替你思考 | 无 AI 原生支持 |

**结论**：Spark 的差异化在于 AI 不替代思考而是催化思考，且围绕"想法生命周期"这一独有数据结构产生别家做不到的洞察（连接、杂交、血脉、思考模式）。

### D. 技术方案概要

```
packages/@spark/ai/
  src/
    provider.ts        # AI Provider 抽象接口
    openai.ts          # OpenAI 实现
    anthropic.ts       # Anthropic 实现（预留）
    local.ts           # 本地模型实现（预留）
    socratic.ts        # 苏格拉底问题生成
    connector.ts       # 连接发现（embedding + LLM）
    catalyst.ts        # 杂交配对选择
    retro.ts           # 回望对话
    mirror.ts          # 思考镜子洞察生成
  embedding/           # Embedding 存储与检索
  index.ts
```

API 路由：

```
/api/ai/socratic           POST  -> 生成追问
/api/ai/connect            POST  -> 发现连接
/api/ai/catalyst           POST  -> 杂交配对
/api/ai/retro              POST  -> 回望对话
/api/ai/mirror             POST  -> 思考镜子
```

---

*文档版本：v1.0 · 2026-08-18 · 状态：Active（P0+P1 全部实现）*
